export class User {

    id:number;
    username:string;
    password:string;
        constructor(user){
            this.id=user.id;
            this.username=user.username;
            this.password=user.password;
    
        }
    
    
    
    }